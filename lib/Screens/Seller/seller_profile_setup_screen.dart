import 'package:flutter/material.dart';

class SellerProfileSetupScreen extends StatelessWidget {
  static String id = 'Seller';
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Seller Profile'),
      ),
    );
  }
}
