import 'package:flutter/material.dart';

class CustomerProfileSetupScreen extends StatelessWidget {
  static String id = 'Customer';
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Customer Profile'),
      ),
    );
  }
}
